# Fast demo configuration for the repo-local LIBERO spatial example.
# This keeps debug output off and targets the currently validated fast path.

segmenter:
  target_name: plate
  prompt_task_info: assets/prompts/libero_spatial/semantic_split_parts/task_info.json
  prompt_image_root: assets/prompts/libero_spatial/semantic_split_parts
  checkpoint_path: checkpoints/sam3.pt
  output_dir: tests/outputs/fast_plate_demo
  overwrite_output: true

  confidence: 0.25
  mask_threshold: 0.6
  prompt_keep_score_threshold: 0.2
  video_mask_prob_threshold: 0.95

  depth_scale: 1000.0
  depth_min: 0.1
  depth_max: 3.0
  stride: 2
  frame_voxel_size: 0.003

  save_ply: false
  save_debug_2d: false

  tracker_image_size: 896
